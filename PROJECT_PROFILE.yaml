profile: web-platform
AI_REQUIRED: false
offline_check: "find js data -name '*.js' -print0 | xargs -0 -n1 node --check && node scripts/check_vocab_data.js && python scripts/ci_check_html_refs.py"
run_command: "see README for the documented build or static preview command"
rollback: "restore the recorded Git commit or deployment"
owner: "repository maintainer"